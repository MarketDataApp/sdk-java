/**
 * Wire-format adapters for the {@code utilities} resource group: the {@code /status/} parallel-
 * arrays shape and the {@code /headers/} flat-object shape with arbitrary keys. Mirrors the
 * structure of {@link com.marketdata.sdk.internal.wire.markets}.
 */
@NullMarked
package com.marketdata.sdk.internal.wire.utilities;

import org.jspecify.annotations.NullMarked;
