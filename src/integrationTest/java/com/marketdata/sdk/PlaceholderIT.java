package com.marketdata.sdk;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so the {@code integrationTest} source set compiles before any real integration tests
 * exist. Replace with live-API tests in subsequent iterations. Gated by {@code
 * MARKETDATA_RUN_INTEGRATION_TESTS=true}.
 */
class PlaceholderIT {

  @Test
  void sourceSetCompiles() {
    // Intentionally empty.
  }
}
